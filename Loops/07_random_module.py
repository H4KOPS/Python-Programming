import random
from tkinter import N
"""
# random() - returns random float between 0.0 and 1.0 (excluded)
print(random.random())

# randit(a,b) => returns random int betwwen a and b (both included)
print(random.randint(10, 25))
"""
"""
nums = [10,4,3,9]
# choice() => returns a random item from the list sequence
print(random.choice(nums))
"""

fruits = ["Apple", "Orange", "Mango"]
# shuffle() => returns the elements shuffeled in random order
print(random.shuffle(fruits))
print(fruits)
